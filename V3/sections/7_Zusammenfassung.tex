本实验将温度对象近似为 $G_1(s) = 16.33/(24s + 1)$ 的 PT1 模型，并针对 $D = 0.69$、$\omega_n = \SI{0.055}{\radian\per\second}$ 的期望 PT2 闭环设计了带积分作用的补偿控制器。仿真显示系统超调较小，并在约 $\SI{100}{\second}$ 的量级接近设定值。真实对象还受到 $T_1$、空气流量、传感器、执行器限幅和非线性热传递的影响。由于部分原始数据缺失，相关章节只给出符合实验要求的分析方法，不虚构结果。
