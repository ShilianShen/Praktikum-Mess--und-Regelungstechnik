本实验搭建、设计并校准了一条完整的热电温度测量链。AD595 提供带冷端补偿的输出电压，该电压通过同相放大器调整到 8 位 AD 转换器的 $\SIrange{0}{5}{\volt}$ 输入范围。由此理论温度分辨率从约 $\SI{1.96}{\celsius}$ 改善到约 $\SI{0.47}{\celsius}$。线性回归得到 $m = [\,]\,\si{\volt\per\celsius}$、$b = [\,]\,\si{\volt}$ 和 $r = [\,]$。相对于参考值的最大偏差为 $[\,]\,\si{\celsius}$。由 $R = [\,]$ 和 $C = [\,]$ 构成的 RC 低通滤波器将干扰衰减了 $[\,]\,\si{\decibel}$。总体而言，测量链[满足/未满足]线性度、精度和抗干扰方面的预期要求。可靠评价的关键在于本组实测数据以及对剩余偏差的可追溯讨论。
