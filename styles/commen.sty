% =========================================================
% Gemeinsamer Einstieg / 公共入口
% =========================================================
% Keep main.tex simple:
%   \usepackage{../styles/commen}
%
% The actual settings live in focused modules. The order matters:
% fonts first, references/links last.
\NeedsTeXFormat{LaTeX2e}

\RequirePackage{../styles/mrt-fonts}
\RequirePackage{../styles/mrt-layout}
\RequirePackage{../styles/mrt-title}
\RequirePackage{../styles/mrt-content}
\RequirePackage{../styles/mrt-references}
